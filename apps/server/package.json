{
  "name": "@human-agent-playground/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm --prefix ../../packages/core run build && npm --prefix ../../games/xiangqi run build && npm --prefix ../../games/gomoku run build && tsc --noEmit -p tsconfig.json",
    "start": "tsx src/index.ts",
    "dev": "tsx watch src/index.ts",
    "test": "npm --prefix ../../packages/core run build && npm --prefix ../../games/xiangqi run build && npm --prefix ../../games/gomoku run build && vitest run"
  },
  "dependencies": {
    "@human-agent-playground/core": "file:../../packages/core",
    "@human-agent-playground/game-gomoku": "file:../../games/gomoku",
    "@human-agent-playground/game-xiangqi": "file:../../games/xiangqi",
    "@modelcontextprotocol/sdk": "^1.27.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4"
  }
}
